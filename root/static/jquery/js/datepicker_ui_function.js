$(function(){

     
            // Datepicker Schedule Estimate (Update Estimate) Form 
            $('#estimate_date').datepicker(
                {
                inline: true,
                dateFormat: "yy-mm-dd",
		showAnim:   "clip" 
            });

            // Datepicker  Find estimates by date form
            $('#schedule_date').datepicker(
                {
                inline: true,
                dateFormat: "yy-mm-dd",
		showAnim:   "clip" 
            });
            
            // Datepicker  Find estimates by date form
            $('#start_date').datepicker(
                {
                inline: true,
                dateFormat: "yy-mm-dd",
		showAnim:   "clip" 
            });

           // Datepicker  Find estimates by date form
            $('#end_date').datepicker(
                {
                inline: true,
                dateFormat: "yy-mm-dd",
		showAnim:   "clip" 
            });

           // Datepicker Generic Input Date Form 
            $('#input_date').datepicker(
                {
                inline: true,
                dateFormat: "yy-mm-dd",
		showAnim:   "clip" 
            });


        });
